---
services:
  portainer:
    image: portainer/portainer-ee:lts@sha256:18750221de87f489fffc3d52a23799e9c6a83aab0f1a5337824c81e12bf552b5
    container_name: portainer
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    volumes:
      - portainer-data:/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      # escape hatch when traefik is down; allowlisted in docker_published_ports
      - 9443:9443
    labels:
      - traefik.enable=true
      - traefik.http.routers.portainer.rule=Host(`portainer.local.elmurphy.com`)
      - traefik.http.routers.portainer.tls=true
      - traefik.http.services.portainer.loadbalancer.server.port=9000
    depends_on:
      - traefik
    restart: unless-stopped

  docker-socket-proxy:
    image: ghcr.io/tecnativa/docker-socket-proxy:v0.5.0@sha256:1f5038b54f06c3e18422902cf00ba21803d1c97805aae032e5e6673d532d3459
    container_name: docker-socket-proxy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    # entrypoint renders haproxy.cfg into /tmp; haproxy writes its pidfile to /run
    tmpfs:
      - /tmp
      - /run
    networks:
      - docker-socket-proxy
    environment:
      - CONTAINERS=1
      - EVENTS=1
      - PING=1
      - POST=0
      - VERSION=1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:2375/version"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped

  # Second proxy instance for the ai-dev Hermes agent. Kept separate from the
  # Traefik proxy above so that widening the agent's read surface never widens
  # Traefik's. POST=0 refuses every write route, including exec and create.
  # Published only on docker-host's tailnet address (AGENT_PROXY_BIND, written
  # into .env by the docker-host role) so the LAN cannot see it at all.
  docker-socket-proxy-agent:
    image: ghcr.io/tecnativa/docker-socket-proxy:v0.5.0@sha256:1f5038b54f06c3e18422902cf00ba21803d1c97805aae032e5e6673d532d3459
    container_name: docker-socket-proxy-agent
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    # entrypoint renders haproxy.cfg into /tmp; haproxy writes its pidfile to /run
    tmpfs:
      - /tmp
      - /run
    networks:
      - docker-socket-proxy-agent
    environment:
      # Read-only diagnostics: ps, logs, stats, inspect, events, images,
      # networks, volumes. POST=0 is the write barrier.
      - CONTAINERS=1
      - EVENTS=1
      - IMAGES=1
      - INFO=1
      - NETWORKS=1
      - PING=1
      - VERSION=1
      - VOLUMES=1
      - POST=0
      # Explicit denials; the image defaults these off, but the agent's
      # blast radius is worth stating rather than inheriting.
      - EXEC=0
      - BUILD=0
      - COMMIT=0
      - CONFIGS=0
      - DISTRIBUTION=0
      - NODES=0
      - PLUGINS=0
      - SECRETS=0
      - SERVICES=0
      - SESSION=0
      - SWARM=0
      - SYSTEM=0
      - TASKS=0
    ports:
      # Tailnet address only; reachability is further pinned to ai-dev in
      # DOCKER-USER by the docker-host role.
      - ${AGENT_PROXY_BIND}:2375:2375
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:2375/version"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped

  traefik:
    image: traefik:v3.7.13@sha256:f86a2cab1b5c649070c49f883c743dd32d8485a56e3368c5f93b9e91f1e91259
    container_name: traefik
    security_opt:
      - no-new-privileges:true
    networks:
      proxy:
        aliases:
          # Portainer exchanges OAuth tokens server-side using the public
          # issuer URL. Resolve it to Traefik's TLS listener inside Docker.
          - pocket-id.local.elmurphy.com
          # Collabora fetches per-user settings from Nextcloud's public URL.
          # Keep that HTTPS request inside Docker instead of hairpinning out.
          - nextcloud.local.elmurphy.com
      docker-socket-proxy: {}
    environment:
      - CF_DNS_API_TOKEN=${CF_DNS_API_TOKEN}
      - CF_API_EMAIL=${CF_API_EMAIL}
    volumes:
      - traefik-data:/letsencrypt
      - ./traefik.yml:/traefik.yml:ro
      - ./traefik-dynamic.yml:/traefik-dynamic.yml:ro
    ports:
      - 80:80
      - 443:443
    labels:
      - traefik.enable=true
      - traefik.http.routers.traefik-secure.entrypoints=websecure
      - traefik.http.routers.traefik-secure.rule=Host(`traefik.local.elmurphy.com`)
      - traefik.http.routers.traefik-secure.tls=true
      - traefik.http.routers.traefik-secure.tls.certresolver=cloudflare
      - traefik.http.routers.traefik-secure.tls.domains[0].main=local.elmurphy.com
      - traefik.http.routers.traefik-secure.tls.domains[0].sans=*.local.elmurphy.com
      - traefik.http.routers.traefik-secure.service=api@internal
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    depends_on:
      - docker-socket-proxy
    restart: unless-stopped

  pocket-id:
    image: ghcr.io/pocket-id/pocket-id:v2.14.0@sha256:01540977dcf4c7b41b1159f34d68e4632f2658d62790e460ca65a42722b13c4a
    container_name: pocket-id
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    environment:
      - APP_URL=https://pocket-id.local.elmurphy.com
      - TRUST_PROXY=true
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - MAXMIND_LICENSE_KEY=${MAXMIND_LICENSE_KEY}
      - PUID=1000
      - PGID=1000
      - ANALYTICS_DISABLED=true
    volumes:
      - pocketid-data:/app/data
    labels:
      - traefik.enable=true
      - traefik.http.routers.pocket-id.rule=Host(`pocket-id.local.elmurphy.com`)
      - traefik.http.routers.pocket-id.tls=true
      - traefik.http.services.pocket-id.loadbalancer.server.port=1411
    healthcheck:
      test: "wget --quiet --tries=1 --spider http://localhost:1411/healthz"
      interval: 1m30s
      timeout: 5s
      retries: 2
      start_period: 10s
    depends_on:
      - traefik
    restart: unless-stopped

volumes:
  portainer-data:
    name: portainer-data
  traefik-data:
    name: traefik-data
  pocketid-data:
    name: pocketid-data

networks:
  proxy:
    name: proxy
    ipam:
      config:
        - subnet: 172.20.1.0/24
  docker-socket-proxy:
    name: docker-socket-proxy
    internal: true
  # Not internal: this proxy publishes a port on the tailnet address.
  docker-socket-proxy-agent:
    name: docker-socket-proxy-agent

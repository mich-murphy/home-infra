apiVersion: v1
kind: Service
metadata:
  name: linkding
  namespace: default
  # annotations:
  #   external-dns.alpha.kubernetes.io/hostname: linkding.mich-murphy.com
spec:
  ports:
    - port: 9090
  selector:
    app: linkding
  type: LoadBalancer

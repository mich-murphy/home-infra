apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-elmurphy-com
  namespace: gateway
spec:
  secretName: local-elmurphy-com-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.local.elmurphy.com"

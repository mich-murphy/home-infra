---
- hosts: docker
  become: true
  vars_files:
    - "group_vars/secrets.yml"
  roles:
    - common
    - firewall
    - mounts
    - geerlingguy.docker
    - portainer

- hosts: games
  become: true
  vars_files:
    - "group_vars/secrets.yml"
  roles:
    - common
    - geerlingguy.docker


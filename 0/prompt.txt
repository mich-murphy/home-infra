I've made a couple of pushes since. Just double-check what's on `main` and confirm that the above command is still the right one to run. The conflict is still there. We can't rebase, so it needs resolution still on the PR.
I agree with this setup, I want you to configure it all for me. You can ssh into proxmox (ssh root@proxmox) and setup main branch protection for the repo. Should we also setup a dedicated access key (or other authentication mechanism) for ai-dev so it's not using my fully authorised key? This should be configured and managed by ansible if so. For docker, I would prefer to work with the already present proxy
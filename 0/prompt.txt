# Annotations on pi · last message

## Annotation 1 (lines 10–14)
Comment on: "- `docker/media-broker/compose.yml` tracks **`:main` unpinned** — documented as the one deliberate exception to your digest-pinned house style - New **`media-broker-autoupdate` systemd timer** (Ansible-owned, `docker-host` role): every 5 min pulls `:main` via host Docker CLI; only when the running container's image differs, fires Portainer's **local** webhook to redeploy. Portainer stays the sole deployment controller — the timer never runs Compose. Credentials live in a root-only `0600` env file written from vault-protected vars; docker login state is isolated in `/var/lib/media-broker-autoupdate` - Rollback: disable the timer, pin a `:sha-<commit>` via Portainer's editor - Verified locally: shellcheck, stack test (now also asserts the timer script's policy), drift check, actionlint, ansible syntax, ansible-lint **production profile** - Pushed: `broker-extraction` branch on home-infra (2 commits), plus the README fix on media-broker"
> Clarify why the auto update in a systemd timer is necessary. Based on my understanding of how Portainer is working, it should pull in the latest update without any intervention separately.

## Annotation 2 (lines 18–22)
Comment on: "1. Create a classic PAT (`read:packages`) for `mich-murphy` 2. **Merge the home-infra PR** (open it from `broker-extraction` — say the word and I'll create it). Must merge *before* step 3, since the stack points at the compose on `main` 3. Portainer: add ghcr.io registry creds → delete old `media-broker` stack → recreate as Git stack (`docker/media-broker/compose.yml`, same env vars) → **enable the stack webhook, copy the URL** 4. Add vault vars `docker_media_broker_autoupdate_github_token` + `docker_media_broker_autoupdate_webhook`, flip `docker_media_broker_autoupdate_enabled: true`, run the `docker-host` role 5. Full-pipeline test is written into the runbook (step 8): merge a trivial media-broker change → `journalctl -u media-broker-autoupdate.service` should show pull → webhook → new image ID"
> Create the PR on home-infra.

## Annotation 3 (lines 18–22)
Comment on: "1. Create a classic PAT (`read:packages`) for `mich-murphy` 2. **Merge the home-infra PR** (open it from `broker-extraction` — say the word and I'll create it). Must merge *before* step 3, since the stack points at the compose on `main` 3. Portainer: add ghcr.io registry creds → delete old `media-broker` stack → recreate as Git stack (`docker/media-broker/compose.yml`, same env vars) → **enable the stack webhook, copy the URL** 4. Add vault vars `docker_media_broker_autoupdate_github_token` + `docker_media_broker_autoupdate_webhook`, flip `docker_media_broker_autoupdate_enabled: true`, run the `docker-host` role 5. Full-pipeline test is written into the runbook (step 8): merge a trivial media-broker change → `journalctl -u media-broker-autoupdate.service` should show pull → webhook → new image ID"
> Portainer is already set up for the home-info repo. Surely, given that it uses the same org login, it already has access to any new media-broker repo we create.